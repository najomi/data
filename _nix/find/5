link: http://user.su/index.php/Bash:Как_поменять_права_группы_файлов 
desc: |
  Для того, чтобы рекурсивно поменять права на каталог и
  вложенные подкаталоги, необходимо выполнить 2 команды: 
code: |
  $ find . -type d -exec chmod 775 "{}" \;
  $ find . -type f -exec chmod 664 "{}" \;
