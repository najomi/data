desc: |
  Отключить внутренний кэш для всех запросов в рамках данной сессии.
  При разработке благодаря этому будет всегда видно реальное время выполнения
  запроса
code: |
  SET SESSION query_cache_type=0;

